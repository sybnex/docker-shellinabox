stages:
- name: build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: sybex/docker-shellinabox
      pushRemote: true
      registry: index.docker.io
timeout: 60
notification: {}
